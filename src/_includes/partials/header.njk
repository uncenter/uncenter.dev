<header class="min-w-[30%] p-4 font-display">
    <div class="flex items-center flex-col">
        <a href="/"
                {% if page.url === "/" %}
                aria-current="page"
                {% endif %}>
            {{ "src/_includes/duck.svg" | dumpContents | setAttr("class", "w-12 h-12 m-auto") | safe }}
            <span class="tracking-tight text-2xl">uncenter</span></a>
        <div class="max-w-[40ch] text-center mt-1">{{ meta.description }}</div>
        <ul class="list-none px-0 py-4">
            {% for item in nav.top %}
                {% set classes = "font-semibold px-2" %}
                {% if page.url | includes(item.urls)and item.urls[0] != "/" %}
                    {% set classes = classes + " [border-left:2px_solid_currentColor]" %}
                {% elif item.urls[0] == "/" and page.url == "/" %}
                    {% set classes = classes + " [border-left:2px_solid_currentColor]" %}
                {% else %}
                    {% set classes = classes + " [border-left:2px_solid_transparent]" %}
                {% endif %}
                <li>
                    <a href="{{ item.urls[0] }}" class="{{ classes }}"
                        {% for link in item.urls -%}
                        {% if page.url === link %}
                        aria-current="page"
                        {% endif %}
                        {%- endfor -%}>{{ item.title }}
                    </a>
                </li>
            {% endfor %}
        </ul>
    </div>
</header>