<!doctype html>
<html lang="en">
  <head>
    {% include "src/_includes/head.njk" %}
  </head>
  <style>
    .content {
      max-width: 800px;
      margin: auto;
    }
  </style>
  <body>
    <div class="content">
      {% if subdir %}
      <div>{% include "src/_includes/subdir_title.njk" %}</div>
      {% else %}
      <h1><a href="/">/uncenter</a><a href="/{{linktitle}}">/{{linktitle}}</a></h1>      
      {% endif %}
      <aside class="hello-message">
        {% for message in hello | randomItem %}
          <span title="{{ message.language }}"><p>{{ message.hello }}! 👋</p></span>
        {% endfor %}
      </aside>
      <!-- 
        hidden duck!
        <code>                        __
                                    /` ,\__
                                  |    ).-'
                                  / .--'
                                / /
                  ,      _.==''`  \
                .'(  _.='         |
                {   ``  _.='       |
                {    \`     ;    /
                  `.   `'=..'  .='
                    `=._    .='
                      '-`\\`__
                          `-._{
              \/\\/\/\\/\/\\\\/\\\\\////\\\///\\/\\//\\\/\/\\/\/\\\/\/\\/\/\/\\//
        </code>

      -->
      {{ content | safe }}
    </div>
  </body>
    <div class="content">
      <footer>
        {% include "src/_includes/foot.njk" %}
      </footer>
  </div>
</html>