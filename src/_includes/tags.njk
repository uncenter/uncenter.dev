{% set separator = "·" %}

<div class="flex flex-row flex-wrap justify-between small-text gap-2">
    <div class="flex flex-row flex-wrap gap-2">
        <time datetime="{{ DATE | toDateTime }}">{{ DATE | toShortDate }}</time>
        {{ separator }}
        <span>~{{ CONTENT | readingTime }} ({{ CONTENT | wordCount }} words)</span>
        {{ separator }}
        <span>{{ VIEWS }} reads</span>
    </div>
    <div class="flex flex-row flex-wrap gap-3">
        {%- if TAGS -%}
            {%- for tag in TAGS -%}
                <a href="/tags/{{ tag | slugify }}" class="no-underline">{%- icon 'tag', class='mr-[0.1em] align-middle' -%}
                    <span class="underline"
            >{{ tag }}</span></a>
            {%- endfor -%}
        {%- endif -%}
    </div>
</div>