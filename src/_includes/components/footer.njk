<div class="flex flex-col items-center self-center w-full gap-6">
    <ul class="flex items-center gap-3 justify-center m-0 p-0 list-none">
        {% for item in nav.bottom %}
        <li>
            <a class="not-fancy" href="{{ item.url }}" rel="{% if item.relme %}me {% endif %}noreferrer" style="color: {{ item.color }}">{% icon item.icon, class="w-5 h-5 block" %}</a>
        </li>
        {% endfor %}
    </ul>
    <div class="flex flex-row text-center gap-x-2 text-xs">
        <a href="https://xn--sr8hvo.ws/previous" class="not-fancy">←</a>
        <a href="https://xn--sr8hvo.ws" class="not-fancy">IndieWeb Webring 🕸💍</a> 
        <a href="https://xn--sr8hvo.ws/next" class="not-fancy">→</a>
    </div>
    <select id="theme-selector" class="text-center text-fg bg-bg border-2 border-gray-300 rounded-md focus:outline-none">
        <option value="light">Light</option>
        <option value="dark">Dark</option>
        <option value="system">System</option>
    </select>
</div>