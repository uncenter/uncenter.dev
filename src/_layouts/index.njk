<!DOCTYPE html>
<html lang="en">
    <head>
        {% include "partials/head.njk" %}
    </head>
    <body class="flex flex-col justify-center mx-4 my-12 bg-bg text-fg font-serif min-h-screen">
        {{ content | safe }}
    </body>
    <script src="/assets/scripts/copy.js"></script>
    {% if scripts %}
        {% for script in (scripts | toArray) %}
            <script src="/assets/scripts/{{script}}"></script>
        {% endfor %}
    {% endif %}
</html>