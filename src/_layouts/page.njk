---
layout: index.njk
---
<div class="flex flex-col lg:flex-row">
    {% include "partials/header.njk" %}
    <main class="max-w-[680px] p-[1em] mx-auto">
        {% if title %}
            <h1 class="text-3xl font-bold break-words">{{ title }}</h1>
        {% endif %}
        {% if content %}
            {{ content | safe }}
        {% endif %}
    </main>
</div>
{% include "partials/footer.njk" %}
{% iconSheet %}