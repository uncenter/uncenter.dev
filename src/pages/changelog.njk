---
title: Changelog
description: A list of recent changes I've made to this site.
---

<dl>
    {%- for group in collections.recentChanges -%}
        <dt class="font-semibold text-center">{{ group[0] }}</dt>
        <dd>
            <ul>
                {%- for commit in group[1] %}
                    {% set message %}
                        {% if commit.subject.split(':').length > 2 %}
                            {% set emoji = "" %}
                            {% for i in range(2, commit.subject.split(':').length) %}
                                {% set emoji = emoji + ':' + commit.subject.split(':')[i] %}
                            {% endfor %}
                            {{ emoji.trim() | toHtmlInline | safe }}
                        {% else %}
                            {{ commit.subject.split(":") [1] }}
                        {% endif %}
                    {% endset %}
                    {% set category = commit.subject.split(':')[0] %}
                    <li data-date-rel="{{ commit.committerDateRel }}"
                        data-date="{{ commit.committerDate }}"
                        data-commit="{{ commit.hash }}"
                        data-category="{{ category }}">
                        <span class="inline-card" data-category="{{ category }}">{{ category }}</span>:
                        {% if commit.subject -%}
                            <a aria-label="View commit on GitHub" href="https://github.com/{{ meta.author.github.username }}/{{ meta.repo }}/commit/{{ commit.hash }}">{{ message }}</a>{%- endif -%}
                        </li>
                    {%- endfor -%}
                </ul>
            </dd>
        {%- endfor -%}
    </dl>
